# Bytedance — prompt patterns (excerpt)

- Pattern: "system + template + variables" — keep system brief and use templates for variable data.
- Use explicit output schemas when you need structured replies (JSON Schema examples included).

(Collected from Bytedance public examples and internal notes)