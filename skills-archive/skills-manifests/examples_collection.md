# Skill manifest examples collection

This directory collects small, common schema fragments used in skill manifests across projects (name, inputs, outputs, version).

Example snippet:
```
inputs:
  - name: text
    type: string
outputs:
  - name: summary
    type: string
```
