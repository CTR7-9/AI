# Skill manifest: example (skill.yaml)

name: "summarizer"
version: "0.2.0"
description: "Summarizes long documents into bullets"
inputs:
  - name: text
    type: string
outputs:
  - name: summary
    type: string

(Template collected from public agent/skill repositories)