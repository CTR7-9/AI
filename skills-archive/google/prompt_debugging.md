# Prompt debugging tips (Google)

- Reproduce failing examples with a fixed seed and model version.
- Minimize prompt length and iteratively add constraints to isolate the failure.
- Log intermediate model outputs when using multi-step prompts to inspect where the divergence occurs.

(Excerpted from google prompt engineering notes)