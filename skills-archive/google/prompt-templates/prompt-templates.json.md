# prompt-templates.json (excerpt)
{
  "summarize": {
    "description": "Produce a concise summary of a document.",
    "template": "Summarize the following text in {length} sentences:\n\n{text}"
  },
  "extract_entities": {
    "description": "Extract named entities and their types.",
    "template": "List entities in the text and their types (PERSON, ORG, LOCATION, DATE):\n\n{text}"
  }
}

(Excerpted from assorted prompt template collections)