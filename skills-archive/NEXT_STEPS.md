# README: next steps for this branch

What I will do next (ongoing work):

1. Continue harvesting more prompt/skill files across the target organizations and repositories.
2. For each harvested file: save a full copy (when license and repo allow) under skills-archive/<org>/<repo>/<path> along with a small metadata JSON containing source URL, commit OID, and capture date.
3. Commit in manageable batches (by repo or ~50 files per commit) and report each commit with the list of files and commit link.
4. After a large enough harvest, optionally create a PR to merge the archive branch into main (or produce a ZIP export as you prefer).

If you'd like a different structure or specific metadata fields in each metadata file, tell me now — otherwise I'll proceed with the plan above.

