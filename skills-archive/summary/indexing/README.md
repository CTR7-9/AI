# Archival indexing README

This directory contains harvested prompt/skill excerpts. Each batch is recorded and added to the manifest. To regenerate an index, run the ingest script and update the manifest with new entries.

(Internal index guidance for the archive project)