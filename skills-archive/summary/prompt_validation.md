# Prompt validation checklist (excerpt)

- Validate outputs against the provided JSON schema.
- Run a small set of unit prompts to ensure backward compatibility when updating templates.

(Validation guidance for prompt changes)