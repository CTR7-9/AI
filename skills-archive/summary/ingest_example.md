# Ingest script usage (example)

To add newly harvested files into the archive index:

```
./scripts/ingest.sh --source=./harvested --manifest=skills-archive/manifest-2026-07-16.md
```

This normalizes file paths and updates the main manifest.
