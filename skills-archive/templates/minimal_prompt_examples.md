# Minimal prompt examples (excerpt)

- Keep minimal examples for common tasks to reduce token cost and improve robustness.
- Example: "Summarize in one sentence: {text}" with a JSON schema for the output.

(Short template examples)