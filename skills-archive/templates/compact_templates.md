# Compact templates (excerpt)

- Use short structured templates for common operations to reduce prompt length and cost.  
- Example: "Briefly summarize: {text} | max_sentences=1 | format=json"

(Compact prompt templates)