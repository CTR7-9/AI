# ModelScope — evaluation README (excerpt)

This README documents how to run model evaluation harnesses using a set of prompt templates and expected-output cases. Use reproducible seeds and record model version metadata.

(Excerpted from ModelScope public examples)