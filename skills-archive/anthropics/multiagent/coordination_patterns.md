# Multi-agent coordination patterns (excerpt)

- Use a coordinator agent to assign tasks and collect results from workers.
- Prefer idempotent worker operations and include sequence IDs to avoid duplication.

(Design patterns for coordinating subagents)