# Multi-agent: fallback policies

- Define light-weight cached responses for common safe queries when agents fail.
- Escalate to human reviewer when confidence < threshold and query touches safety categories.

(Fallback policy examples)
