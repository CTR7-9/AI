# Aggregation patterns for multi-agent outputs

- Concatenate structured outputs and run a schema validator.
- Use voting with weights based on agent confidence.
- Run a final verifier agent to check factual claims.

(Excerpted from multi-agent orchestration notes)