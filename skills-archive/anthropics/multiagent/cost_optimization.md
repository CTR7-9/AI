# Multi-agent cost optimization (excerpt)

- Use conditional execution: only invoke expensive subagents when the simpler ones are uncertain.
- Track per-agent cost metrics and adjust routing dynamically.

(Operational notes)