# Multi-agent latency mitigation (excerpt)

- Run researcher agents in parallel and cap per-agent timeout to bound latency.
- Cache intermediate results when possible to avoid repeated work.

(Excerpted from multi-agent system guides)