# Multi-agent consensus patterns (excerpt)

- Use weighted voting based on agent confidence and past accuracy.
- Include tie-breaker rules and an explicit fallback when consensus fails.

(Excerpted from multi-agent coordination notes)