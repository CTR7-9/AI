# Example skill manifest (skill.yaml excerpt)

name: "citation_finder"
version: "0.1.0"
description: "Finds and formats citations from input text using a citation style."
inputs:
  - name: text
    type: string
outputs:
  - name: citations
    type: array

(Excerpted from multiple agent/skill manifests found in public repos)