# Safety filters: simple examples

- Block or flag inputs containing "bypass", "exploit", or "evade".
- Rate-limit requests that try iterative probing of filters.
- Route crisis-related queries to humans with high priority.

(Example guardrail snippets)