# Safety logging examples (excerpt)

- Log rule triggers with a short hashed input and reason code to preserve privacy.
- Keep a separate audit log with full inputs accessible only to authorized reviewers when required.

(Operational examples for safety logging)